To setup, run `sudo make setup`. This sets file permissions (usually uses `chmod +x`), and therefore needs only be run once.

After this, `make` to initialize all databases and insure that all is well.
